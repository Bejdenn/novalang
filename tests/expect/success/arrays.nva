// lists are implemented as array lists, so no size
x: int[];

// array literals
x = [1, 2, 3];

int::print(x[0]);

// access array values by index
z := x[2];

str::print("z: " + str(z));

fn ok(arr: int[]): void {
    arr[0] = 200;
}

ok(x);
int::print(x[0]);

x[0] = 99;
int::print(x[0]);